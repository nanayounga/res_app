<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nganth.restaurantapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.SignInActivity" />
        <activity android:name=".user.SignUpActivity" />
        <activity android:name=".user.VerificationActivity" />
        <activity android:name=".restaurant.WalkthoughtActivity" />
        <activity android:name=".search.SearchActivity" />
        <activity android:name=".search.ResultActivity" />
        <activity android:name=".restaurant.MenuActivity" />
        <activity android:name=".restaurant.AboutActivity" />
        <activity android:name=".user.FavoriteActivity" />
        <activity android:name=".user.SettingActivity" />
        <activity android:name=".user.ProfileActivity" />
        <activity android:name=".restaurant.ReviewActivity" />
        <activity android:name=".user.ChangePasswordActivity"></activity>
    </application>

</manifest>